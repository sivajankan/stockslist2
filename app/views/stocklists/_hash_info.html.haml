%table.table-bordered.table-striped.table-nonfluid{'width'=>'600px'}
  %tbody
  - #debugger
  - @inhash.first[1].each do |key, val|
    - next if key == 'symbol'
    %tr
      %td{'width'=>'200px', 'align'=>'right'}=t("stockcodes.#{key}", :default => key)
      %td{'width'=>'150px'}= val
    


